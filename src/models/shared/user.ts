export type T_UserId = number

export enum E_UserRole {
  superAdmin = 'superAdmin',
  admin = 'admin',
  manager = 'manager',
  client = 'client',
}

export enum E_UserStatus {
  resident = 'resident',
  president = 'president',
  vicePresident = 'vicePresident',
  assemblyCouncilMember = 'assemblyCouncilMember',
}
